# Backend Placeholder

Planned stack:

- FastAPI
- MQTT subscriber for garbage telemetry
- PostgreSQL
- Shortest path service exposed with `GET /route`
- WebSocket live updates for node status

The frontend is already prepared to consume `GET /nodes` and `GET /route`.
